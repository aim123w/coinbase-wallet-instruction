/* Сброс стилей по умолчанию */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Основные стили для страницы */
body {
    font-family: Arial, sans-serif;
    line-height: 1.6;
    background-color: #e0f7fa; /* Светло-голубой фон */
    color: #333;
    padding: 0 15px;
}

header {
    background: #00796b; /* Тёмно-зелёный цвет для заголовка */
    color: #fff;
    text-align: center;
    padding: 20px;
    border-radius: 8px;
    margin-bottom: 20px;
}

h1 {
    font-size: 1.8em;
}

h2 {
    font-size: 1.5em;
    color: #00796b; /* Тёмно-зелёный цвет для подзаголовков */
    margin-bottom: 10px;
}

p {
    margin-bottom: 15px;
}

strong {
    color: #d32f2f; /* Красный цвет для выделенного текста */
}

.content {
    background: #fff;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
}

.step {
    margin-bottom: 30px;
}

.step img {
    max-width: 50%; /* Ограничение максимальной ширины изображения до 50% от ширины контейнера */
    height: auto; /* Автоматическая высота для сохранения пропорций */
    border-radius: 8px;
    margin-top: 10px;
    /* Для альтернативного подхода, можете использовать transform:
    transform: scale(0.5); 
    transform-origin: top left; 
    */
}

footer {
    text-align: center;
    padding: 10px;
    margin-top: 20px;
    background: #00796b;
    color: #fff;
    border-radius: 8px;
}

