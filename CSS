/* Сброс стилей по умолчанию */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Основные стили для страницы */
body {
    font-family: Arial, sans-serif;
    line-height: 1.6;
    background-color: #e0f7fa; /* Светло-голубой фон */
    color: #333;
    padding: 0 15px;
}

header {
    background: #00796b; /* Тёмно-зелёный цвет для заголовка */
    color: #fff;
    text-align: center;
    padding: 20px;
    border-radius: 8px;
    margin-bottom: 20px;
}

h1 {
    font-size: 1.8em;
}

h2 {
    font-size: 1.5em;
    color: #00796b; /* Тёмно-зелёный цвет для подзаголовков */
    margin-bottom: 10px;
}

p {
    margin-bottom: 15px;
}

strong {
    color: #d32f2f; /* Красный цвет для выделенного текста */
}

main {
    margin-bottom: 30px;
}

.step {
    margin-bottom: 30px;
    max-width: 100%; /* Убедитесь, что контейнер не ограничивает размеры изображения */
    overflow: hidden; /* Скрыть излишки */
}

.step img {
    transform: scale(0.1667); /* Уменьшение изображения в 6 раз */
    transform-origin: top left; /* Точка отсчёта для трансформации */
    max-width: none; /* Убираем ограничение на максимальную ширину */
    height: auto; /* Автоматическая высота для сохранения пропорций */
    border-radius: 8px;
    margin-top: 10px;
}

footer {
    text-align: center;
    padding: 10px;
    margin-top: 20px;
    background: #00796b;
    color: #fff;
    border-radius: 8px;
}
